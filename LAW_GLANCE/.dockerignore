# Gradle build outputs (rebuilt inside container)
build/
app/build/
.gradle/

# IDE & OS files
.idea/
.vscode/
*.iml
*.DS_Store
Thumbs.db

# Local SDK paths (not needed inside Docker)
local.properties

# Git
.git/
.gitignore
