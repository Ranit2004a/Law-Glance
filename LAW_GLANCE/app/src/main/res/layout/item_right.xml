<?xml version="1.0" encoding="utf-8"?>
<androidx.cardview.widget.CardView
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginHorizontal="10dp"
    android:layout_marginVertical="6dp"
    app:cardCornerRadius="16dp"
    app:cardElevation="4dp"
    app:cardBackgroundColor="#CC1F1F2E"> <!-- Translucent premium slate background -->

    <LinearLayout
        android:orientation="vertical"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:padding="16dp">

        <!-- Title + arrow -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:gravity="center_vertical">

            <TextView
                android:id="@+id/rightTitle"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:text="Right Title"
                android:textSize="16sp"
                android:textColor="#FFFFFF"
                android:textStyle="bold" />

            <ImageView
                android:id="@+id/expandArrow"
                android:layout_width="24dp"
                android:layout_height="24dp"
                android:src="@drawable/ic_arrow_down_vector"
                android:tint="#FFFFFF"
                app:tint="#FFFFFF"
                android:contentDescription="Expand Arrow"/>
        </LinearLayout>

        <!-- Details (expandable) -->
        <LinearLayout
            android:id="@+id/detailsLayout"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:visibility="gone"
            android:paddingTop="12dp">

            <!-- Divider line inside card -->
            <View
                android:layout_width="match_parent"
                android:layout_height="1dp"
                android:background="#222233"
                android:layout_marginBottom="12dp"/>

            <TextView
                android:id="@+id/rightDescription"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:text="Description"
                android:textColor="#BCBCC4"
                android:textSize="14sp"
                android:lineSpacingExtra="3dp"
                android:paddingBottom="6dp"/>

            <TextView
                android:id="@+id/rightCause"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:text="Cause / Violation"
                android:textColor="#FF7043"
                android:textSize="14sp"
                android:lineSpacingExtra="3dp"
                android:paddingBottom="6dp"/>

            <TextView
                android:id="@+id/rightPunishment"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:text="Punishment / Fine"
                android:textColor="#81C784"
                android:textSize="14sp"
                android:lineSpacingExtra="3dp"
                android:paddingBottom="4dp"/>
        </LinearLayout>

    </LinearLayout>
</androidx.cardview.widget.CardView>
