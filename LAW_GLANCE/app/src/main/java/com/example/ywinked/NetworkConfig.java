package com.example.ywinked;

public class NetworkConfig {
    // Auto-updated by start_dev.ps1 each session.
    // After this script runs, rebuild the APK in Android Studio.
    public static final String BASE_URL = "https://actually-explained-formal-embassy.trycloudflare.com";
}
